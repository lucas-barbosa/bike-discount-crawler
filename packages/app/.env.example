# Stock Scheduler Configuration
# How often the stock scheduler runs (in days)
STOCK_SCHEDULER_INTERVAL_DAYS=2

# Skip products if stock changed within this time (in hours)
STOCK_SKIP_IF_CHANGED_WITHIN_HOURS=24

# Category Scheduler Configuration
# How often the category scheduler runs (in days)
CATEGORY_SCHEDULER_INTERVAL_DAYS=30

# Redis Configuration
# REDIS_HOST=localhost
# REDIS_PORT=6379

# BullMQ Configuration
# BULLMQ_CONCURRENCY=5
